apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: rxpulse-dev
  labels:
    app: user-service
    app.kubernetes.io/part-of: rxpulse
    app.kubernetes.io/managed-by: kubectl
    environment: dev
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
