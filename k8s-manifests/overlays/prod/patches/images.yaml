# This file is intentionally kept as a placeholder.
# Image overrides are now configured directly in kustomization.yaml
# under the `images:` key, which is the correct Kustomize pattern.
# Strategic merge patches cannot contain `images:` directives.
